# Optional. When set, pages are built from Sanity instead of local migrated content.
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Only needed for the seed script (never commit this).
SANITY_WRITE_TOKEN=
