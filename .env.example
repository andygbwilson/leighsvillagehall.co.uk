# Optional override. Defaults to project lc8qwfpd / production.
PUBLIC_SANITY_PROJECT_ID=lc8qwfpd
PUBLIC_SANITY_DATASET=production

# Only needed for the seed script (never commit this).
SANITY_WRITE_TOKEN=
