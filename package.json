{
  "name": "leighs-village-hall",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "studio": "npm run dev --prefix studio",
    "studio:deploy": "npm run deploy --prefix studio",
    "seed": "node --experimental-strip-types --disable-warning=ExperimentalWarning scripts/seed-sanity.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@sanity/client": "^7.12.0",
    "astro": "^7.3.2",
    "groq": "^4.10.2"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
